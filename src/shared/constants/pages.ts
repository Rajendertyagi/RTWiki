// The autosave debounce is defined once in constants/index.ts
// (PROVISIONAL_AUTOSAVE_DEBOUNCE_MS); never re-declare it here.
export const MAX_TITLE_LENGTH = 200
export const DEFAULT_PAGE_TYPE = 'rich' as const
